<p style="color: green"><%= notice %></p>

<h1>Shows</h1>

<div id="shows">
  <% @shows.each do |show| %>
    <%= render show %>
    <p>
      <%= link_to "Show this show", show %>
    </p>
  <% end %>
</div>

<%= link_to "New show", new_show_path %>
